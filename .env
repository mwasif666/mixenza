# Live domains — loaded automatically by `next build` / `next start`.
# No secrets here: both values are public and end up in the browser bundle.
NEXT_PUBLIC_SITE_URL=https://mixenza.com,http://localhost:3001
NEXT_PUBLIC_API_URL=https://backend.mixenza.com/api
